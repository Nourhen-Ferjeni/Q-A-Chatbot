# 🧠 Q&A Chatbot with LangChain

This project is a Question & Answer chatbot powered by **LangChain**, **OpenAI embeddings**, and **Chroma** as a vector database.  
You will load documents, create embeddings, and query them through a conversational interface.

---

## 📌 Project Requirements

To run this project, you must have:

- A **valid OpenAI API key**
- **Jupyter Notebook** installed
- A **Python environment (>= 3.9 and < 4.0)**  
  It is strongly recommended to create a new virtual environment dedicated to this project.

---

## 📦 Required Python Packages

Install the following versions for optimal compatibility:

```bash
pip install langchain==0.3.3
pip install langchain-chroma==0.1.4
pip install langchain-community==0.3.2
pip install langchain-openai==0.2.2
pip install pypdf==5.0.1
pip install python-dotenv==1.0.1
